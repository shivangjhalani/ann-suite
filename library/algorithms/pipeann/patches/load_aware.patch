diff --git a/src/search/pipe_search_common.h b/src/search/pipe_search_common.h
index 3e9fc81..8d313df 100644
--- a/src/search/pipe_search_common.h
+++ b/src/search/pipe_search_common.h
@@ -8,6 +8,7 @@
 #endif

 #include "candidate_pool.h"
+#include "utils/io_governor.h"

 namespace pipeann {

@@ -38,6 +39,7 @@ namespace pipeann {
                                              std::vector<Neighbor> &full_retset, QueryStats *stats,
                                              InsertContext *insert_ctx, float range_partial, NodeOut *node_out) {
     QueryBuffer *query_buf = pop_query_buf(query1);
+    io_gov.active.fetch_add(1, std::memory_order_relaxed);
 #ifdef USE_URING
     void *ctx = reader->get_ctx(IORING_SETUP_SQPOLL);
 #else
@@ -403,7 +405,7 @@ namespace pipeann {

     // --- Main search loop ---
     auto cpu2_st = std::chrono::high_resolution_clock::now();
-    send_best_read_req(cur_beam_width - on_flight_ios.size());
+    send_best_read_req(io_gov.cap(cur_beam_width) - on_flight_ios.size());

     if (mem_L) {  // Overlap the PQ query-table build with the first read.
       nbr_handler->initialize_query(query, query_buf);
@@ -429,7 +431,7 @@ namespace pipeann {
         }
       }

-      if ((int64_t) on_flight_ios.size() < cur_beam_width) {
+      if ((int64_t) on_flight_ios.size() < io_gov.cap(cur_beam_width)) {
         send_best_read_req(1);
       }
       marker = calc_best_node();
@@ -451,6 +453,7 @@ namespace pipeann {
               [](const Neighbor &left, const Neighbor &right) { return left < right; });

     push_query_buf(query_buf);
+    io_gov.active.fetch_sub(1, std::memory_order_relaxed);

     if (stats != nullptr) {
       stats->total_us = (double) query_timer.elapsed();
diff --git a/src/utils/linux_aligned_file_reader.cpp b/src/utils/linux_aligned_file_reader.cpp
index 5c54701..f69b441 100644
--- a/src/utils/linux_aligned_file_reader.cpp
+++ b/src/utils/linux_aligned_file_reader.cpp
@@ -1,3 +1,4 @@
+#include <cstdlib>
 #include <bits/types/struct_iovec.h>
 #include "ssd_index_defs.h"
 #include "utils/lock_table.h"
@@ -620,6 +621,10 @@ void *LinuxAlignedFileReader::get_ctx(int flag) {
 void LinuxAlignedFileReader::register_thread(int flag) {
   if (ioctx::ring == nullptr) {
     ioctx::ring = new io_uring();
+    {
+      const char *e = std::getenv("PIPEANN_SQPOLL");
+      if (e != nullptr && std::atoi(e) == 0) flag &= ~IORING_SETUP_SQPOLL;
+    }
     int ret = io_uring_queue_init(MAX_EVENTS, ioctx::ring, flag);
     if (ret != 0) {
       LOG(ERROR) << "io_uring_queue_init failed: " << strerror(-ret);
diff --git a/tests/CMakeLists.txt b/tests/CMakeLists.txt
index a73c111..7a536d9 100644
--- a/tests/CMakeLists.txt
+++ b/tests/CMakeLists.txt
@@ -19,6 +19,8 @@ target_link_libraries(overall_perf_mem ${PROJECT_NAME})

 add_executable(search_disk_index search_disk_index.cpp)
 target_link_libraries(search_disk_index ${PROJECT_NAME})
+add_executable(search_openloop search_openloop.cpp)
+target_link_libraries(search_openloop ${PROJECT_NAME})

 add_executable(search_disk_index_mem search_disk_index_mem.cpp)
 target_link_libraries(search_disk_index_mem ${PROJECT_NAME})
